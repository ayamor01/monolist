<!DOCTYPE html>
<html>
<head>
  <% if content_for?(:title) %>
    <title><%= yield :title %> - monolist</title>
  <% else %>
    <title>Monolist</title>
  <% end %>
  <% if content_for?(:item_description) %>
    <meta name="description"
      content="<%= yield :item_description %>に関する情報です。この商品を欲しい人、持っている人を表示しています。"/>
  <% elsif content_for?(:user_description) %>
    <meta name="description"
      content="<%= yield :user_description %>のモノページ。<%= yield :user_description %>の欲しいモノ、持ってるモノを表示しています。"/>
  <% end %>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <%= favicon_link_tag 'favicon.ico' %>
  <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no" />
</head>
<body>

<%= render partial: "layouts/header" %>
<%= yield %>
<%= render partial: "layouts/footer" %>

</body>
</html>
